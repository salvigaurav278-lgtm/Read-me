# Copy to keystore.properties and fill in. Generate a keystore with:
#   keytool -genkeypair -v -keystore app/release.keystore -alias <alias> \
#     -keyalg RSA -keysize 2048 -validity 10000
storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
